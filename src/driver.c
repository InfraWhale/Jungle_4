#include "rbtree.h"

// #include <stdio.h> // 추후삭제

int main(int argc, char *argv[]) {

// int key = 128;

// rbtree *t = new_rbtree();
// node_t *p = rbtree_insert(t, key);

// printf("root : %d \n", t->root->key);
// printf("p : %d \n", p->key);

// rbtree_erase(t, p);

// printf("p : %d \n", p->key);

// if (t->root == t->nil) {
// printf("good \n");
// }

// delete_rbtree(t);

}